test("Dashboard displays patients and day entrees", async () => {});

test("user can add entress to patient order", async () => {});

test("user can remove entress from patient order", async () => {});
